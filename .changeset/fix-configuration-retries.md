---
'react-native-sse-hooks': patch
---

Stop retrying synchronous request configuration failures by default, retire partially initialized requests, and keep package verification aligned with the current manifest version.
